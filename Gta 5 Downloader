<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Download GTA 5</title>
<style>
/* Reset default styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body styling */
body {
    font-family: 'Arial', sans-serif;
    background: #111;
    color: #fff;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 20px;
}

/* Container */
.container {
    max-width: 400px;
    width: 100%;
    text-align: center;
}

/* GTA icon styling */
#gta-icon {
    width: 150px;
    height: 150px;
    margin-bottom: 20px;
}

/* Heading */
h1 {
    font-size: 2em;
    margin-bottom: 10px;
}

/* Paragraph */
p {
    font-size: 1em;
    color: #ccc;
    margin-bottom: 20px;
}

/* Dropdown */
select {
    width: 100%;
    padding: 12px;
    font-size: 16px;
    border-radius: 6px;
    border: none;
    margin-bottom: 20px;
}

/* Button styling */
button {
    width: 100%;
    padding: 15px;
    font-size: 18px;
    cursor: pointer;
    background: linear-gradient(90deg, #00ffcc, #00b3b3);
    border: none;
    border-radius: 10px;
    color: #111;
    font-weight: bold;
    transition: all 0.3s ease;
}

button:hover {
    transform: scale(1.05);
    background: linear-gradient(90deg, #00e6b8, #009999);
}

/* Message display */
#message {
    font-size: 16px;
    margin-top: 25px;
    color: #ff5555;
    min-height: 24px;
    transition: all 0.3s ease;
}

/* Responsive adjustments */
@media (max-width: 500px) {
    h1 {
        font-size: 1.6em;
    }
    button {
        font-size: 16px;
    }
}
</style>
</head>
<body>

<div class="container">
    <!-- GTA 5 Icon -->
    <img id="gta-icon" src="https://upload.wikimedia.org/wikipedia/en/4/44/GTA_V.png" alt="GTA 5 Icon">

    <h1>Download GTA 5 for Free</h1>
    <p>Select your device and click download:</p>

    <label for="device" class="sr-only">Select Device</label>
    <select id="device">
        <option value="android">Android</option>
        <option value="ios">iOS</option>
        <option value="windows">Windows</option>
    </select>

    <button id="downloadBtn">Download</button>
    <p id="message"></p>
</div>

<script>
// Elements
const downloadBtn = document.getElementById('downloadBtn');
const messageElement = document.getElementById('message');

downloadBtn.addEventListener('click', () => {
    // Loading message
    messageElement.style.color = "#00ffcc";
    messageElement.textContent = "Preparing download...";

    // Fake download delay
    setTimeout(() => {
        const messages = [
            "Sorry, GTA 5 can't be downloaded here",
            "Oops! Something went wrong, try again later",
            "Your download is temporarily unavailable"
        ];
        const msg = messages[Math.floor(Math.random() * messages.length)];
        messageElement.style.color = "#ff5555";
        messageElement.textContent = msg;
    }, 2000);
});
</script>

</body>
</html>
